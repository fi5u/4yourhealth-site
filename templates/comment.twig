<div class="comments__comment {{comment.comment_type}}" id="blog-comment-{{comment.ID}}">
	<h5 class="comments__author">{{comment.author.name}}</h5>
	<div class="comments__content">{{comment.comment_content|wpautop}}</div>
</div>